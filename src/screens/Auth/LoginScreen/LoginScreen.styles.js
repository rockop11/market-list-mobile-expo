import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: "center",
        justifyContent: "center",
    },
    title: {
        paddingTop: 10,
        fontSize: 32,
        fontWeight: "bold",
        textAlign: "center"
    },
    textContainer: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        padding: 10,
    },
    register: {
        fontWeight: 'bold',
        fontSize: 16,
        // color: ""
    }
})