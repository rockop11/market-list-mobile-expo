import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputsContainer: {
        width: "100%",
        flexDirection: 'row',
        alignItems: 'center',
        gap: 8,
    },
    button: {
        borderRadius: 10,
        backgroundColor: '#02d31e'
    },
})